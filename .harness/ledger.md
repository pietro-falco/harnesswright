# harnesswright — slice ledger

Verifier is `@pietro-falco/verity` `>=0.1.0` from S1 onward. Evidence is raw
stdout only — `git show HEAD:<path>`, `cat -n`, exit codes, verity receipts —
never prose. This ledger lived at `docs/ledger.md` through S7 (self-hosting)
and now lives at `.harness/ledger.md`.

| ID | Slice | Expected atomic commits | Acceptance criteria | Required evidence | Gate |
|----|-------|--------------------------|----------------------|--------------------|------|
| S0 | Recon | read-only, no commits | Path free, verity resolvable via `npx`, global git email risk identified | Raw command output | Passed 2026-07-02 |
| S1 | Scaffold + ADR Proposed | 5 (scaffold, hooks, ADR-001, ledger, verity manifest) | Repo-local dev identity set before first commit; pre-commit hook fires; ADR committed verbatim | `git log`, `cat -n`, hook run output | Human ADR review (done in chat) — Passed 2026-07-02 |
| S2 | ADR flip + spec | 2 | `docs/spec.md` covers `harness.json` schema, emitted-file contract, gate exit codes 0/1/2 | `cat -n docs/spec.md` | Human spec review — Passed 2026-07-02 |
| S3 | init core | 3 | Pure `plan()` + `emit`, `--dry-run`/`--force`/`--yes`, embedded templates v1, `node --test` green | Test run stdout | Deterministic — Passed 2026-07-02 |
| S4 | gate command | 3 + 1 incident fix (self-recursive gate claim removed) | Fixture repos: pass → 0, fail → 1, config error → 2; exit codes propagated from verity | Fixture run exit codes | Deterministic — Passed 2026-07-02 |
| S5 | doctor | 2 | Detects missing git, inactive `hooksPath`, unresolvable verity, duplicate branch checkouts; read-only | Doctor run stdout | Deterministic — Passed 2026-07-02 |
| S6 | Adoption layer | 4 (ledger, own contract, npm packaging, verity claims) | `AGENTS.md` + `SKILL.md` templates; npm files whitelist; `SKILL.md` in tarball (`npm pack --dry-run`) | `npm pack --dry-run` output | Deterministic — Passed 2026-07-02 |
| S7 | Self-hosting + CI | 5 (ledger, init adoption, curation, CI from source, verity claims) | Repo adopts its own harness; `gate.yml` matrix Node 20/22/24; CI green | CI run link/log | Human verified CI green on GitHub — Passed 2026-07-02 |
| S8 | Publish-readiness v0.1.0 | ~7 (ledger, README, SECURITY, CHANGELOG, OSINT demo, version bump, publish-readiness claims + tag) | README complete (one-line thesis, 60-sec quickstart, comparison table vs Spec Kit / dynamic workflows, non-goals, built-with-itself with the three incidents); OSINT demo passes its own gate; version 0.1.0; all 18+ root claims green | `git show` of files; full-repo verity receipt; demo gate exit 0; `git tag` output | Human: README review in chat, then manual `npm publish` + push — Passed 2026-07-03 (npm page live, tag v0.1.0 pushed, CI green at c847567) |
| S9 | Trusted publishing (OIDC) | 4 (ledger, CHANGELOG in tarball, release workflow, verity claims) | Package exists on npm after manual S8 publish; trusted publisher configured on npmjs.com afterwards; provenance automatic on the next release; publishing stays operator-initiated via tag | npmjs.com provenance badge; workflow run URL | Human configures trusted publisher on npmjs.com — Passed 2026-07-03 (0.1.1 live with SLSA provenance attestation `slsa.dev/provenance/v1`; OIDC trusted publishing via tag-triggered `release.yml`) |
| S10* | ADR-002: `next` command (optional, post-launch) | 2+ (Proposed docs-only -> review -> Accepted + implementation slices) | `next` reads `.harness/harness.json` (slice sequence + per-slice `status` field as the sole completion source; the ledger .md is never parsed) and prints which slice is unlocked and what is missing; read-only scheduling, no execution; consistent with ADR-001's execution non-goal | ADR blob; implementation slice with tests | Human ADR-002 review — Accepted 2026-07-06; implementation gate: `node --test` green + human review |
| S11 | ADR-003: per-slice spec schema | 2 (Proposed docs-only -> review -> Accepted); implementation slices only after Accepted | Per-slice specs `.harness/specs/S<id>.md` as executable contracts: machine-readable frontmatter (`mode` A\|B, `efficiency` skill list, `effort` low\|high, `budget` tokens/turns/wall-clock, `stop_conditions` with non-removable `gate-failure`, `criteria` referencing verity claim IDs); `harness.json` stays sole completion source; no execution/orchestration, no writes by `next`, no auto-retry on failed gate | ADR blob (`git show HEAD:docs/adrs/0003-per-slice-spec-schema.md`) | Human ADR-003 review in chat — Accepted 2026-07-10 (review in chat) |

| S12 | ADR-004 implementation: spec validator + Mode B eligibility in `next` | 3 (validator, `next` extension, dogfooding) | `.harness/specs/<id>.md` frontmatter validated per ADR-003 + ADR-004 D1 (restricted YAML, zero deps; unknown key or invalid value = exit 2; `gate-failure` unioned and non-removable; `scope` required for mode B); `next --json` reports `eligible_mode_b` as a total predicate per D2 (accepted mode-B spec ∧ no `.harness/locks/<id>.lock`, presence-only, content never parsed) and the effective model tier per D8; `next` stays read-only, exit vocabulary 0/2 unchanged; this slice's own spec validates against the validator it ships | `node --test` stdout, `tsc --noEmit` exit code, `git show HEAD:.harness/specs/S12.md` | Deterministic — `node --test` green + `npm run build` clean |

\*S10 must not open before v0.1.0 is published and stable.
